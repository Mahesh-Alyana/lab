name: dump
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  any_link_preview: ^2.0.7
  badges: ^2.0.2
  built_value: ^8.1.4
  cached_network_image: ^3.0.0
  carousel_slider: ^4.0.0
  connectivity: ^3.0.6
  cupertino_icons: ^1.0.2
  device_info_plus: ^3.2.2
  dio: ^4.0.4
  double_back_to_close_app: ^2.0.1
  file_picker: ^4.5.1
  floating_bottom_navigation_bar: ^1.5.2
  flutter:
    sdk: flutter
  flutter_screenutil: ^5.3.1
  flutter_slidable: ^1.2.0
  get: ^4.6.1 #  For managing state and routes
  get_storage: ^2.0.3
  google_fonts: any # Select font family
  image_cropper: ^2.0.0
  image_picker: ^0.8.5
  image_viewer: ^1.5.0
  intl: ^0.17.0
  json_annotation: ^4.4.0
  logger: ^1.1.0  # Print Logs in outputs
  numeric_keyboard: ^1.1.0  #Plugin to numeric Keyboard for otp inputs
  open_file: ^3.2.1
  pattern_formatter: ^2.0.0 # Validate inputs in text fields
  flutter_pdfview: ^1.2.1
  permission_handler: ^8.3.0 # Handle different types of permission in app
  retrofit: ^3.0.1+1
  socket_io_client: ^1.0.2
  syncfusion_flutter_pdfviewer: ^20.1.52-beta
  excel: ^2.0.0-null-safety-3
  flutter_windowmanager: ^0.2.0

dev_dependencies:
  build_runner: ^2.1.8
  change_app_package_name: ^1.0.0
  flutter_launcher_icons: ^0.9.2
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter
  json_serializable: ^6.1.5
  retrofit_generator: ^4.0.1

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/img/logo.png"


flutter:

  uses-material-design: true
  assets:
    - assets/img/
    - assets/img/logo.png
    - assets/whatsapp_Back.png